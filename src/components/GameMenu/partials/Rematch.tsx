import Button from "~/components/Common/Button";

const Rematch = () => {
  return (
    <Button
      onClick={() => {
        console.log("first");
      }}
    >
      Rematch
    </Button>
  );
};

export default Rematch;
